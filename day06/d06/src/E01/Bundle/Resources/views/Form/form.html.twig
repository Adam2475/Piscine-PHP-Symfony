{% extends 'base.html.twig' %}

{% block body %}

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit">submit</button>
    {{ form_end(form) }}

    <button><a href="/">back</a></button>

{% endblock %}