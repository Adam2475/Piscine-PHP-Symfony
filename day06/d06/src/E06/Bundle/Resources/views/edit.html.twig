{% extends 'base.html.twig' %}

{% block body %}
    <h1>Edit Post</h1>

    {{ form_start(form) }}
        {{ form_row(form.title) }}
        {{ form_row(form.content) }}
        {{ form_row(form.save) }}
    {{ form_end(form) }}

    <a href="{{ path('homepage') }}">Back to homepage</a>
{% endblock %}
